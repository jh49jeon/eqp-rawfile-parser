# Equipment Raw File Parser

**Capstone 트랙**: 트랙 B — 자동화 Agent · Workflow
**구현에 쓸 수 있는 시간**: 코칭 전후·저녁 + 내일 발표 전까지 (실질 6~8시간)

## ① 문제 정의 — 지금 무엇이 불편한가
- 누가 / 언제 / 어떤 일에서: PHRMS 시스템 백엔드 개발자 / 설비 구조 변경이 있을 때 / 변경된 설비 FILE 구조에 맞춘 Parser 수정이 필요함
- 줄이는 비용 (반복 / 대기 / 검증 중 하나): 반복
- 지금 걸리는 시간·횟수: 수백 번 / 건당 약 30분

## ② AI 에이전트에게 맡길 부분
- 에이전트가 대신할 일: 업로드된 전/후 파일 2개 diff로 변경된 바이트 구간 확인 → 사용자 변경 진술(예: 25.0℃ → 30.0℃)과 대조해 그 구간의 오프셋·타입·인코딩을 추론 → **schemaMaker가 알아낸 파라미터를 schema.json에 한 건씩 적립** → 적립된 스키마로 parser 로직 수정, 엔지니어 피드백 확인 후 검증 완료까지 반복 수정
- 사람이 계속 할 일: 실제 설비 값 수정 후 전/후 파일 업로드 및 변경 사항 전달, **파서 호출 규약을 먼저 고정** — `parse(path) -> [{unit, table, row, column, value, dtype, offset}, ...]` 형태의 평탄한 레코드 리스트로 확정 (Tester가 파서 코드를 안 보고도 실행 가능한 테스트를 쓰려면 선행되어야 함), 엔지니어의 자유 텍스트 피드백을 기계가 읽을 수 있는 요구사항 형식으로 번역
- 쓸 도구 (서브에이전트·MCP·스킬·hook·병렬 중 필요한 것만): 서브에이전트(Code Review), 서브에이전트(Tester — PyTest 작성 담당, 파서 작성자와 분리. **입력을 변경 요구사항과 샘플 A/B 바이너리로만 제한하고 파서 코드·schema.json에는 접근하지 않는다** — 기대값을 요구사항에서만 도출시켜 자기채점을 막기 위함), 서브에이전트(데이터 생성 — Phase 0 샘플 A/B 생성 담당), 서브에이전트(schemaMaker — A/B diff와 사용자 변경 진술로부터 파라미터 정의를 추론해 schema.json에 적립), Claude Hook(사용자 업로드 원본 바이너리 파일 수정 방지를 위한 read-only 파일 접근 제한)

## ③ 범위 — 반드시 / 되면 좋은 / 안 하는 것
- 반드시 (이게 안 되면 실패): 생성된 parser 로직을 이용하여 PyTest 자동화 테스트 통과
- 되면 좋은: 현재 확정된 raw file 업로드 시 parsing 결과 확인하기 (테이블 형태)
- 이번엔 안 하는 것: FTP 연동하여 파일 가져오기, 테이블 구조(로우·컬럼) 자체가 변경되는 경우의 대응 — 전/후 파일 사이에 구조는 바뀌지 않음이 확정됨

## ④ Phase 분할과 각 Phase 완료 기준
0. 테스트용 샘플 raw file(전/후 A/B 세트)과 백데이터 생성 — 담당: 데이터 생성 서브에이전트 — 완료: 서브에이전트가 샘플 A/B를 생성하고, 두 파일이 **값 하나만** 다르다 (설비에서 값만 변경한 경우 테이블 구조는 바뀌지 않음을 상정하므로, 테이블 구조는 A와 B가 동일하다)
1. 전/후 파일 diff 위치 파악 후 schemaMaker가 파라미터 정의 적립 — 완료: schema.json이 없으면 새로 생성되고, 있으면 기존 항목과의 키 중복 검사에서 상충 없이 유니크한 파라미터명으로 추가됨. 적립된 항목의 오프셋 구간이 기존 항목과 겹치지 않음 (PyTest assertion 통과)
2. Parsing 방식 선정 — 완료: 사용자 변경 요구사항(예: 25.0℃ -> 30.0℃)과 파싱 결과값의 일치 여부를 검증하는 PyTest 테스트 케이스 통과
3. 엔지니어 피드백 반영 및 Parser 수정 — 완료: 사람이 자유 텍스트 피드백을 요구사항 형식으로 번역해 제출하면, 그 요구사항 파일을 입력으로 Tester가 생성한 PyTest가 수정된 파서에 대해 통과 (피드백 문장 자체는 기계 판정 대상이 아니며, 번역된 요구사항만 판정 대상이다)

## ⑤ 완료를 판정할 방법 (검증 게이트)
- 기계가 판정하는 것: 생성된 parser 로직 및 스키마 검증 PyTest 자동화 스크립트 통과 (Assertion Success)
- 사람이 눈으로 보는 것: 전/후 파일 parsed data (표 형태) 확인
- 내가 직접 승인할 지점: AI가 생성/수정한 파서 로직 및 스키마를 메인 코드베이스에 Merge하기 직전

## GitHub 저장소 URL
(로컬 작업 후 생성 예정)

## 1:1 코칭에서 가장 묻고 싶은 것
Claude Agent가 파서 코드 작성 후 PyTest를 자동으로 실행하고, 실패 시 스스로 스키마 및 코드를 교정하는 하네스 피드백 루프를 최적으로 설계하는 방법

---
(아래는 폼에 넣지 않는 작업 메모)

## 되돌아갈 지점과 데이터
- 되돌리는 방법: Git 커밋/태그 기준 롤백
- 실데이터·자격증명 없이 되게 만드는 방법 (샘플로 대체할 부분): 샘플 바이너리 파일(A/B 세트) 직접 생성하여 테스트

## 대상 파일 포맷 (사용자 설명 그대로)
- 바이너리 파일. 디스플레이 photo track 장비의 생산 recipe 관련 파일.
- 각 유닛별 파라미터 값이 들어감. 값의 타입은 int, long, timestamp 등 다양함.
- 데이터가 매트릭스(테이블) 형태이며 컬럼명·로우명도 존재함 (이것도 파싱 대상).
- key와 value가 고정 포맷으로 들어가지 않고, **크기가 가변인 테이블 형태**로 들어감.

### 구조 정보의 출처 (확정)
- 파일에는 **값만** 들어가며, 구조를 기술하는 헤더가 없다. 컬럼명·로우명이 개념적으로 존재하지만 **파일 안에서 문자열로 읽어낼 수 있는 형태가 아니다.**
- 따라서 구조는 파일에서 복원되지 않고 **바깥에서 주입**된다. 유일한 정보원은 **A/B 한 쌍 + 사용자의 변경 진술**이다. A와 B는 값 하나만 다르므로(Phase 0 전제), 달라진 바이트 구간이 그 파라미터의 위치이고 진술("25.0℃ → 30.0℃")이 타입·인코딩을 알려준다.
- schema.json은 **처음에 존재하지 않고** schemaMaker가 변경 이벤트마다 한 건씩 적립하며 자라난다.
- **사용자가 줄 수 있는 것은 넷뿐이다** — 전/후 파일, 바꾼 파라미터의 **유닛**, **파라미터명**, **변경 전/후 값**. 엔지니어는 설비 CIM PC에서 자기가 바꾼 내용만 알 뿐 파일 내부의 테이블 구성이나 컬럼·행 배치는 모른다. 따라서 `table`·`row`·`column`·`dtype`·`offset`·`endian`은 입력이 아니라 **schemaMaker의 산출물**이며, 명세 어디에서도 사용자에게 요구하지 않는다.
- **테이블 구조는 전/후 파일 사이에서 바뀌지 않는다(확정).** A와 B는 바이트 길이가 같고 오프셋이 밀리지 않는다.
- **따라오는 귀결: 한 번의 A/B로는 바뀐 파라미터 하나만 알게 된다.** 파일의 나머지 영역은 미지의 바이트로 남고, 파서의 출력은 "지금까지 알아낸 파라미터 집합"이다. 파일 전체를 파싱하는 것이 아니다.

## 고칠 곳 (4단계 반박 결과)
- [반영 완료] Phase 1/2 완료 기준: 단순 문자열 대조가 아닌 PyTest assertion 검증 방식으로 구체화함.
- [반영 완료] 사용자 파일 보호: Claude Hook을 통해 업로드 항목을 read-only로 제한하도록 명확화함.

### 2차 반박 (검토자 시점)
- [검증] ③ "반드시" · ⑤ 기계 판정 — 파서와 PyTest를 **둘 다 에이전트가 작성**하면 자기가 통과할 테스트를 쓰게 된다. 통과는 자기채점이 되고, 검증 게이트가 게이트 역할을 못 한다. 테스트 케이스(기대값)를 누가 확정하는지가 계약에 없다.
- [범위] ③ · ④ — 계약 전체가 "전/후 raw file"을 전제하는데 **그 파일의 포맷이 어디에도 없다**(고정 길이 바이너리인지, 텍스트인지, 섹션 구조인지). 작업 메모에 "샘플 A/B 세트 직접 생성"이라고만 있고 Phase에는 없다. 샘플 설계가 사실상 Phase 0인데 시간이 배정돼 있지 않다.
- [범위] ④ — Phase 2와 Phase 3의 완료 조건이 사실상 같은 문장이다("변경 요구 조건과 파싱 결과의 일치를 PyTest로 확인"). 두 Phase를 나눈 근거가 완료 기준에 드러나지 않아, 3개 Phase가 아니라 2개로 읽힌다.
- [검증] ④ Phase 3 — "엔지니어 피드백 반영"이 기계 판정 조건이 아니다. 피드백이 자유 문장으로 오면 무엇이 반영됐는지 기계가 판정할 수 없다. 피드백을 어떤 형식으로 받을지가 정해져야 이 Phase가 자동 판정된다.
- [승인] ② · ⑤ — 승인 지점이 "메인 코드베이스 Merge 직전" 하나뿐이다. 그런데 Phase 1에서 에이전트가 **기존 schema.json을 직접 수정**하게 되어 있다. hook은 업로드 원본만 read-only로 막고 schema.json은 막지 않으므로, 되돌리기 어려운 변경이 승인 없이 일어나는 자리다.
- [범위/해소] ④ Phase 0 추가로 샘플 설계에 자리가 생김. 또한 "설비에서 값만 바꾸면 테이블 구조는 안 바뀐다"는 전제를 세워, 가변 테이블에서 바이트 오프셋이 밀리는 문제(로우/컬럼 추가 시 diff가 파일 뒤쪽 전체를 변경으로 표시하는 문제)를 이번 범위에서 제외함. **이 전제가 실제로 깨지는 경우(로우 추가 등)는 "이번엔 안 하는 것"에 아직 명시돼 있지 않음.**
- [검증/해소] 자기채점 문제 — Tester 서브에이전트를 파서 작성자와 분리하고, **Tester의 입력을 변경 요구사항 + 샘플 A/B 바이너리로만 제한**하기로 확정함(파서 코드·schema.json 비접근). 기대값이 파서가 아니라 요구사항에서만 나오므로 PyTest 통과가 게이트 역할을 회복함. Tester는 테스트만 작성하고 파서는 수정하지 않는다.
  - 따라오는 선행 조건: Tester가 파서 코드를 못 보므로 **호출 규약을 사람이 먼저 고정**해야 테스트가 실행 가능하다. `parse(path) -> [{unit, table, row, column, value, dtype, offset}, ...]` 평탄한 레코드 리스트로 확정함.

### 3차 정리 (SPEC 작성 전 확정 사항)
- [해소] 구조 정보의 출처가 확정됨 — 위 "구조 정보의 출처" 절 참조. 이로써 Phase 1의 완료 조건을 "기존 schema.json"이 없는 첫 실행까지 포함하도록 다시 씀.
- [해소] Phase 3 완료 조건을 다시 씀 — 자유 텍스트 피드백은 기계 판정 대상이 아니고, 사람이 번역한 요구사항 파일만 판정 대상이다. 번역이 사람 손에 남으므로 이 부분의 반복 비용은 완전히 사라지지 않는다는 점은 감수한다.
- **[남은 지적] ③ "되면 좋은"과의 충돌** — "raw file 업로드 시 parsing 결과 확인(테이블 형태)"이라고 되어 있으나, 위 귀결에 따라 출력은 파일 전체 테이블이 아니라 **알아낸 파라미터 몇 건**이다. 초기에는 표에 한두 줄만 찍힌다. 이 항목의 기대치를 조정할지는 코칭에서 결정.
- **[남은 지적] 데모 성립성** — 스키마가 변경 이벤트마다 한 건씩 자라므로, 발표 시점에 보여줄 스키마를 채우려면 Phase 0에서 A/B 세트를 **여러 쌍** 만들어야 한다. 현재 Phase 0 완료 조건은 한 쌍만 요구한다.
- [되돌리기] 작업 메모 — "Git 커밋/태그 기준 롤백"으로 적혀 있으나 **이 폴더는 아직 git 저장소가 아니다**(`git rev-parse` 실패 확인). 첫 커밋 전에 에이전트가 코드를 생성하면 되돌아갈 지점이 없다.