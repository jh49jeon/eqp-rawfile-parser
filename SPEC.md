# SPEC — Equipment Raw File Parser

PLAN.md의 실행 계약을 구현 명세로 내린 문서다. 계약과 충돌하는 내용이 있으면 PLAN.md가 우선한다.

---

## 1. 핵심 전제

이 시스템의 성격을 규정하는 전제가 하나 있다. **대상 바이너리에는 값만 들어 있고 구조를 기술하는
헤더가 없다.** 컬럼명·로우명은 개념적으로 존재하지만 파일에서 문자열로 읽어낼 수 없다.

따라서 구조는 파일에서 복원되지 않고 바깥에서 주입된다. 정보원은 하나뿐이다 —
**A/B 파일 한 쌍과 사용자의 변경 진술.** A와 B는 값 하나만 다르므로, 달라진 바이트 구간이
그 파라미터의 위치이고, 진술("25.0℃ → 30.0℃")이 타입과 인코딩을 알려준다.

여기서 두 가지가 따라 나온다.

- **한 번의 A/B는 파라미터 하나를 알려준다.** 파일의 나머지는 미지의 바이트로 남는다.
- **schema.json은 처음에 존재하지 않는다.** 변경 이벤트가 쌓이며 한 건씩 자라난다.

파서의 출력은 "파일 전체의 파싱 결과"가 아니라 **"지금까지 알아낸 파라미터 집합"**이다.

### 사용자가 줄 수 있는 것의 한계

엔지니어는 설비 CIM PC에서 **자기가 무엇을 바꿨는지만** 안다. 파일 내부의 테이블 구성이나
컬럼·행 배치는 알지 못한다. 따라서 입력으로 받을 수 있는 것은 다음 넷뿐이다.

1. 변경 전 파일 (A)
2. 변경 후 파일 (B)
3. 바꾼 파라미터가 속한 **유닛**과 그 **파라미터명** (CIM PC 화면에서 보이는 이름)
4. 변경 **전 값**과 **후 값**

`table` · `row` · `column` · `dtype` · `offset` · `endian`은 **입력이 아니라 산출물**이다.
schemaMaker가 추론해야 한다. 명세 어디에서도 사용자에게 이것들을 요구하지 않는다.

### 테이블 구조 불변 (확정)

전/후 파일 사이에 테이블 구조가 바뀌는 일은 없다. 로우·컬럼의 추가나 삭제는 발생하지 않는다.
따라서 A와 B는 **바이트 길이가 같고**, 오프셋이 밀리지 않는다. 이 전제 위에서만 diff가 성립한다.

---

## 2. 디렉터리 구조

```
eqp-rawfile-parser/
├── PLAN.md
├── SPEC.md
├── schema.json                 # 최초에는 없음. schemaMaker가 생성·적립
├── src/
│   ├── parser.py               # parse() — 호출 규약 고정 대상
│   ├── schema_maker.py         # A/B diff + 진술 → 파라미터 정의 추론
│   └── differ.py               # 바이트 구간 diff
├── samples/                    # ← hook으로 read-only. 에이전트 쓰기 금지
│   ├── set01_a.bin
│   ├── set01_b.bin
│   └── set01_change.yaml       # 해당 세트의 변경 진술
├── tests/
│   └── test_*.py               # Tester 서브에이전트만 작성
└── .claude/
    ├── settings.json           # PreToolUse hook
    └── agents/
        ├── data-gen.md
        ├── schema-maker.md
        ├── tester.md
        └── code-review.md
```

---

## 3. 호출 규약 (고정)

Tester가 파서 코드를 보지 않고 테스트를 쓰려면 이 규약이 먼저 확정되어야 한다.
**이 시그니처는 사람이 고정한 것이며 에이전트가 바꿀 수 없다.**

```python
def parse(path: str, schema: dict) -> list[dict]:
    """알려진 파라미터만 추출한 평탄한 레코드 리스트를 반환한다."""
```

반환 레코드 하나의 형태:

| 필드 | 타입 | 설명 |
|---|---|---|
| `name` | `str` | 파라미터명 — 사용자가 제공 |
| `unit` | `str` | 유닛 식별자 — 사용자가 제공 |
| `value` | `int \| float \| str` | 디코딩된 값 |
| `dtype` | `str` | `int32` / `int64` / `float32` / `timestamp` 등 (추론 결과) |
| `offset` | `int` | 파일 내 바이트 오프셋 (추론 결과) |
| `table` | `str \| None` | 미상이면 `None` |
| `row` | `str \| None` | 미상이면 `None` |
| `column` | `str \| None` | 미상이면 `None` |

`table` · `row` · `column`은 **거의 언제나 `None`이다.** 사용자가 알려줄 수 없고 파일에도 라벨이
없기 때문이다. 필드를 남겨두는 이유는 나중에 알아낼 여지를 열어두기 위해서지, 채워질 것을
기대해서가 아니다. **Tester는 이 세 필드에 대해 assertion을 걸지 않는다.**

레코드 순서는 `offset` 오름차순으로 고정한다 — 순서가 흔들리면 테스트가 불안정해진다.

**미지 영역은 레코드로 내보내지 않는다.** 알아낸 것만 나온다.

---

## 4. schema.json

`parse()`에 주입되는 유일한 구조 정보다. 최초 실행 시 파일이 없으면 schemaMaker가 생성한다.

```json
{
  "version": 3,
  "parameters": [
    {
      "name": "chuck_temp",
      "unit": "COT",
      "offset": 1024,
      "size": 4,
      "dtype": "float32",
      "endian": "little",
      "table": null,
      "row": null,
      "column": null,
      "status": "confirmed",
      "ambiguous": false,
      "rejected_candidates": [],
      "evidence": [ { "set": "set01", "before": "25.0", "after": "30.0" } ],
      "superseded_by": null
    }
  ]
}
```

`status`와 정정 흐름은 4-1절에 따로 적는다.

`name`은 **사용자가 변경 진술에서 부여한다.** CIM PC 화면에서 보이는 이름이므로 사람이 안다.
schemaMaker는 이름을 짓지 않고 받아 적기만 한다. `unit`도 마찬가지다.

`evidence`는 이 항목이 어느 관측에서 나왔는지 남기는 감사 기록이다. 재현 검증에 쓰인다.

**불변 조건 (Phase 1의 기계 판정 대상):**
- `name`이 전체에서 유일하다
- `status`가 `deprecated`가 아닌 항목들끼리 `[offset, offset+size)` 구간이 겹치지 않는다

---

## 4-1. 파라미터의 상태 (status)

한 번 적립된 항목이 나중에 틀렸다고 밝혀질 수 있다. dtype을 자동 해소한 경우가 특히 그렇고,
엔지니어가 파싱 결과를 보고 "이 값은 이게 아니다"라고 지적하는 경우도 있다.
**상태 없이 덮어쓰면 무엇이 왜 바뀌었는지 남지 않으므로**, 항목마다 `status`를 둔다.

| status | 뜻 | 파싱에 쓰이나 |
|---|---|---|
| `confirmed` | 추론이 유일하게 결정됐고 검증도 통과 | 쓰인다 |
| `provisional` | `ambiguous: true`로 자동 해소한 항목. 아직 근거가 약함 | 쓰이되 결과에 표시된다 |
| `disputed` | 사람이 "이 값 틀렸다"고 지적함. 재추론 대기 | **쓰이지 않는다** |
| `deprecated` | 재추론으로 대체됨. 이력 보존용 | 쓰이지 않는다 |

`parse()`는 `confirmed`와 `provisional`만 사용한다. `provisional` 항목에서 나온 레코드는
반환값에 `provisional: true`를 달아 표 출력에서 구분되게 한다.

**정정 흐름**

1. 사람이 파싱 결과를 보고 틀린 항목을 지목한다 → 해당 항목 `status: disputed`
2. 지목할 때 근거를 남긴다 — 그 파라미터의 **올바른 값**(화면에 보이는 그대로)
3. schemaMaker가 기존 `evidence`에 이 새 관측을 더해 **5-1절 탐색을 다시 돌린다.**
   관측이 늘었으므로 후보가 더 좁혀진다 — 이전에 `provisional`이던 것이 확정될 수 있다
4. 새 정의가 나오면 새 항목으로 추가하고, 기존 항목은 `status: deprecated` +
   `superseded_by`에 새 항목을 가리킨다. **삭제하지 않는다**

항목의 `evidence`는 단일 관측이 아니라 **목록**이다. 관측이 쌓일수록 추론이 강해진다.

```json
{
  "name": "chuck_temp",
  "unit": "COT",
  "offset": 1024, "size": 4, "dtype": "float32", "endian": "little",
  "status": "confirmed",
  "ambiguous": false,
  "rejected_candidates": [],
  "evidence": [
    { "set": "set01", "before": "25.0", "after": "30.0" },
    { "set": "set07", "before": "30.0", "after": "22.5" }
  ],
  "superseded_by": null
}
```

**이 정정 자체가 Phase 3이다.** 엔지니어 피드백을 반영해 파서를 고치는 루프가 곧 이 흐름이다.

---

## 5. 변경 진술 형식

사람이 작성한다. Tester와 schemaMaker의 유일한 진실 공급원이다.

사람이 작성하며, **1절의 네 가지 입력만 담는다.** 여기에 없는 항목을 사람에게 묻지 않는다.

```yaml
# samples/set01_change.yaml
set: set01
unit: COT              # 바꾼 파라미터가 속한 유닛
name: chuck_temp       # CIM PC 화면에서 보이는 파라미터명
before: "25.0"         # 변경 전 값 — 화면에 보이는 그대로
after: "30.0"          # 변경 후 값 — 화면에 보이는 그대로
```

**값은 CIM PC 화면에 표시된 문자열 그대로 적는다. 소수점 이하 자리도 보이는 대로 남긴다.**
이것이 dtype 모호성을 푸는 핵심 단서다.

| 화면 표시 | 적는 값 | schemaMaker의 해석 |
|---|---|---|
| `25.0` | `"25.0"` | 소수점이 있으므로 **실수 후보만** 본다 |
| `25` | `"25"` | 소수점이 없으므로 **정수 후보를 우선**한다 |
| `1` | `"1"` | 정수. `0 → 1` 같은 모호한 경우도 정수로 확정된다 |

반드시 따옴표로 감싸 **문자열로** 적는다. YAML이 `25.0`을 숫자로 파싱하면
`25`와 구분되지 않아 이 단서가 사라진다.

`table` · `row` · `column` · `dtype` · `offset` · `endian`은 여기에 없다. schemaMaker가 채운다.

Phase 3의 엔지니어 피드백도 사람이 이 형식으로 번역해 제출한다.
**자유 텍스트 피드백 자체는 기계 판정 대상이 아니다.**

---

## 5-1. schemaMaker 추론 알고리즘

사용자가 `dtype`도 `offset`도 주지 않으므로, schemaMaker는 **탐색으로 찾아낸다.**
이 탐색은 기계가 스스로 정답 여부를 판정할 수 있다 — 그래서 자동화가 성립한다.

1. **길이 확인.** A와 B의 바이트 길이가 다르면 즉시 실패한다 (구조 불변 전제 위반).
2. **차이 구간 추출.** 바이트 단위로 비교해 달라진 구간을 모은다. 구간이 하나가 아니면
   Phase 0 전제 위반이므로 실패로 보고한다 — 추측해서 진행하지 않는다.
3. **후보 인코딩 대입.** 차이 구간을 포함하는 후보 슬라이스에 대해 (`dtype`, `endian`) 조합을
   전부 시도한다. 후보 집합은 `int16` / `int32` / `int64` / `float32` / `float64` /
   `timestamp(epoch32, epoch64)` × (`little`, `big`).
4. **채택 조건.** A의 슬라이스를 디코딩한 값이 `before`와 같고, **동시에** B의 같은 슬라이스가
   `after`와 같은 조합만 채택한다. 한쪽만 맞는 조합은 버린다.
5. **모호성 처리.** 먼저 **표기된 소수점으로 후보를 좁힌다.** 사용자가 적은 값 문자열에
   소수점이 있으면 실수 후보만, 없으면 정수 후보만 본다 (5절 참조). 이것만으로 대부분 풀린다.

   그러고도 조합이 둘 이상 남으면 아래 우선순위로 하나를 고른다.
   작은 정수(예: `0 → 1`)에서는 여러 정수 dtype이 동시에 맞아떨어지므로 이 경우가 드물지 않다.

   | 순위 | dtype | 비고 |
   |---|---|---|
   | 1 | `int32` | **정수를 기본으로 삼는다** |
   | 2 | `int64` | |
   | 3 | `int16` | |
   | 4 | `float32` | 정수 후보가 하나도 없을 때만 |
   | 5 | `float64` | |
   | 6 | `timestamp` | 값의 크기가 epoch 범위일 때만 후보에 오름 |

   endian은 같은 dtype 안에서 `little`을 먼저 고른다.

   **자동 해소했다는 사실을 반드시 남긴다.** 해당 항목에 `ambiguous: true`와 탈락한 후보 목록을
   기록하고, 승인 화면에 표시한다. 잘못 고른 dtype은 이후 모든 파싱을 조용히 오염시키므로,
   자동으로 고르되 사람 눈에는 반드시 걸리게 한다.

   **소수점 표기와 맞는 후보가 하나도 없으면 실패로 보고한다.** 예를 들어 `"25.0"`이라고 적혔는데
   실수 조합이 전혀 안 맞고 정수 조합만 맞는다면, 화면 표기와 저장 형식이 다르다는 뜻이므로
   자동으로 넘어가지 않고 사람에게 확인받는다.
6. **적립.** 유일한 조합이 나오면 4절의 불변 조건(이름 유일, 오프셋 구간 비중첩)을 검사한 뒤
   `schema.json`에 추가한다.

부동소수 비교는 정확한 상등이 아니라 `math.isclose`를 쓴다 — `25.0`이 `float32`에서
정확히 표현되지 않을 수 있다.

---

## 6. 에이전트 경계

여기가 이 프로젝트의 검증 게이트를 지탱하는 부분이다. 경계가 무너지면 PyTest 통과가 자기채점이 된다.

| 에이전트 | 읽을 수 있는 것 | 쓸 수 있는 것 |
|---|---|---|
| data-gen | (없음 — 명세만) | `samples/*.bin`, `samples/*_change.yaml` |
| schema-maker | `samples/`, `src/differ.py` | `schema.json` |
| **tester** | **`samples/*.bin`, `samples/*_change.yaml` 만** | `tests/` |
| parser 작성 | `schema.json`, `src/`, 테스트 **결과**(코드 아님) | `src/` |
| code-review | 전부 | (없음 — 보고만) |

**tester는 `src/`와 `schema.json`을 읽지 않는다.** 기대값을 파서가 아니라 변경 진술에서만
도출시키기 위함이다. tester는 테스트만 쓰고 파서는 절대 수정하지 않는다.

---

## 7. Phase별 산출물과 판정

### Phase 0 — 샘플 생성 (data-gen)
- 산출: `samples/setNN_a.bin`, `setNN_b.bin`, `setNN_change.yaml`

**샘플 크기와 내용.** 파일 크기는 **256KB를 기본**으로 한다. 실제 recipe 파일에 가까운 규모여야
"전체 파일에서 변경된 곳만 찾아낸다"는 것이 의미를 갖는다. 작은 파일에서는 diff가 자명해져
schemaMaker가 제대로 검증되지 않는다.

파일 나머지 영역은 **0으로 채우지 않는다.** 0으로 채우면 변경 구간이 유일한 비영(非零) 영역이
되어 탐색이 무의미해진다. 다음처럼 채운다.

- 여러 유닛·여러 테이블에 해당하는 그럴듯한 값들을 배치한다 (int32, float32, timestamp 혼재)
- **바꾼 파라미터와 같은 값을 가진 바이트열이 파일의 다른 곳에도 존재하게 한다** — 이것이
  없으면 "값으로 검색하면 바로 찾아진다"가 되어 A/B diff를 쓸 이유가 사라진다
- A와 B에서 이 배경 데이터는 **바이트 단위로 완전히 동일**하다
- 판정: A와 B의 바이트 길이가 같고, 다른 바이트 구간이 정확히 한 곳이며,
  그 구간이 `change.yaml`의 `before`/`after`를 생성 시 사용한 인코딩으로 디코딩한 값과 일치한다
  (data-gen은 정답 인코딩을 알고 만들지만, `change.yaml`에는 적지 않는다 — 적으면 schemaMaker의
  탐색이 정답을 훔쳐보게 되어 5-1절이 검증 가치를 잃는다)

### Phase 1 — 스키마 적립 (schema-maker)
- 산출: `schema.json` (없으면 생성, 있으면 항목 추가)
- 판정: 4절의 두 불변 조건을 만족한다

### Phase 2 — 파싱 (parser)
- 산출: `src/parser.py`
- 판정: `parse(b.bin, schema)` 결과에서 해당 `name`의 `value`가 `change.yaml`의 `after`와 같고,
  `parse(a.bin, schema)`에서는 `before`와 같다

### Phase 3 — 피드백 반영 / 잘못된 항목 정정
- 산출: 갱신된 `schema.json` (4-1절 정정 흐름), 필요 시 수정된 `src/parser.py`
- 판정: 사람이 번역해 제출한 요구사항 파일에 대해 Tester가 생성한 PyTest가 통과하고,
  지적된 항목이 `deprecated` + `superseded_by` 연결로 이력이 보존된 채 대체되어 있다

---

## 8. 안전장치

**Hook (PreToolUse).** `samples/` 하위 파일에 대한 Write·Edit를 차단한다.
업로드 원본이 에이전트에 의해 수정되면 A/B 전제 자체가 무너진다.

**승인 지점.** 두 곳이다.
1. `schema.json` 변경 — 되돌리기 어려운 누적 자산이므로 적립 전 사람이 확인
2. 메인 코드베이스 Merge 직전

**되돌리기.** 구현 시작 전 `git init` + 최초 커밋이 선행되어야 한다.
현재 이 폴더는 git 저장소가 아니다.

---

## 9. 열린 항목 (코칭에서 결정)

- **"되면 좋은" 표 출력의 기대치** — 출력은 파일 전체가 아니라 알아낸 파라미터 몇 건이다.
  초기에는 표에 한두 줄만 찍힌다.
- **데모 성립성** — 스키마가 한 건씩 자라므로 발표 시점에 보여줄 스키마를 채우려면
  Phase 0에서 A/B 세트를 여러 쌍 만들어야 한다. 현재 Phase 0 완료 조건은 한 쌍만 요구한다.
- **오프셋 겹침으로 드러나는 오류** — 자동으로 `int32`를 골랐지만 실제로는 `int16`이면,
  옆 오프셋에서 다음 파라미터가 발견될 때 구간이 겹쳐 Phase 1 불변 조건에 걸린다.
  이때 새 항목을 거부할지, 기존 `provisional` 항목을 자동으로 `disputed`로 내릴지가 미정이다.
  (4-1절 정정 흐름은 사람이 지목하는 경우만 다룬다)

> **해소됨** — 테이블 구조는 전/후 파일 사이에서 바뀌지 않음이 확정되었다(1절). 로우·컬럼
> 추가·삭제는 발생하지 않으므로 오프셋이 밀리는 문제는 이 프로젝트에서 고려하지 않는다.
